// "Importing Typescript Types";

// Interface Named Export.
export interface Person {
    username: string;
    email: string;
}

// "type Alias" Named Export.
export type Color = "red" | "green" | "blue";

// Note: "type" Imports are being conducted in "default.ts" file.